
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: nexus
  namespace: nexus
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: nexus.localhost.com
    http:
      paths:
      - backend:
          serviceName: nexus
          servicePort: http
